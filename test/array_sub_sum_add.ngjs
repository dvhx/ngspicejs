#!/usr/bin/env ngspicejs
// Array add sub sum
// linter: ngspicejs-lint
"use strict";

var a = [10, 20, 30];
var b = [1, 2, 3];

echo_json(array_add(a, b));
echo_json(array_sub(a, b));
echo_json(array_sum(a));
echo_json(array_sum(b));
